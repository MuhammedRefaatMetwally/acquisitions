# Production Environment Configuration
NODE_ENV=production
PORT=3000
LOG_LEVEL=info

# Neon Cloud Database Configuration (injected via environment variables)
# DATABASE_URL=postgresql://neondb_owner:password@ep-xxx.neon.tech/neondb?sslmode=require

# JWT Configuration (injected via environment variables)
# JWT_SECRET=your-super-secure-production-jwt-secret

# Arcjet Configuration (injected via environment variables)
# ARCJET_KEY=ajkey_production_key

# Note: In production, these values should be injected via:
# - Container orchestration platform (Kubernetes secrets)
# - Cloud provider secret management (AWS Secrets Manager, etc.)
# - CI/CD pipeline environment variables
# - Docker Compose environment section (for simple deployments)